{
  "name": "snow-rush",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "tsx watch server/index.ts",
    "build": "tsc && vite build",
    "build:server": "tsc -p server/tsconfig.json",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build && npm run build:server"
  },
  "dependencies": {
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/three": "^0.185.4",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "vite": "^8.0.13",
    "vitest": "^4.1.10"
  }
}
